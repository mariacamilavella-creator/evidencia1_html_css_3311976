---
// div : es una etiqueta abierta
// tiene elementos dentro
// permite especificar propiedades de ubicacion


 //Definimos las propiedades
 // que puede recibir el componente
 interface Props {
    etiqueta: string;
    
 }

// crear una variable
// para recibir el componente
// Astro.prost = Los valores desde el control
const etiqueta = Astro.props.etiqueta;
---
<label>{etiqueta}</label>
<div class="rgb">
  
    <div id="cedula">
         <input class="radio-bnt" 
         type="radio"
          name="rgb"
          id="radio1"
       />
     C.C
</div>

<div id="targeta">
      <input class="radio-bnt" 

       type="radio"
       name="rgb"
       id="radio1"
     /> 
     TI
</div>

<div id="extrangeria">
     <input class="radio-bnt"
       type="radio"
       name="rgb"
        id= "radio1"
     /> 
    C.E
     
     <style>
   

      .rgb{
        background-color: rgb(248, 246, 248);
        border-width: 1px;
        border-style: solid;
        border-radius:5px;
        margin:30px 0 10px 10px;
        width:90%;
        border-color: black;
      }
      #cedula{ font-family: Arial, Helvetica, sans-serif;}
      #cedula .radio-bnt{
          background-color: rgb(233, 37, 233);
          display: inline-block;
          width: 10px;
          height: 10px;
         
   }
        
      #targeta{ font-family: Arial, Helvetica, sans-serif;}
        #targeta .radio-bnt{
          background-color: rgb(248, 246, 248);
         display: inline-block;
          width: 10px;
          height: 10px;
          font-family: Arial, Helvetica, sans-serif;
        }
        #extrangeria{ font-family: Arial, Helvetica, sans-serif;}
        #extrangeria .radio-bnt{
         background-color: rgb(248, 246, 248);
         display: inline-block;
          width: 10px;
          height: 10px;
         font-family: Arial, Helvetica, sans-serif;
        }
        
      
     </style>
</div>
 
